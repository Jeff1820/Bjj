/* Curated YouTube demonstration links, keyed by exact checklist item text.
 * Items without an entry fall back to a YouTube search link in the UI.
 * Being populated — see videos in progress. */
const VIDEO_LINKS = {};
